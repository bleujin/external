// view script children sample
new function(){

	importPackage(java.lang) 
	importPackage(net.ion.craken.node.convert)

	this.handle = function(writer, session, cmscontext){
		var dc = cmscontext.getIDBController() ;
		dc.createUserCommand("select userId from user_tblc").execQuery().debugPrint() ;
		return null ;
	}

} ;